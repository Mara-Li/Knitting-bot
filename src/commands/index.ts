import update from "./update";
import configuration from "./config_button";
import ignore from "./ignore";
import follow from "./follow";

export const commands = [ignore, configuration, update, follow];
